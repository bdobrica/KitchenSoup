# Optional override: copy to docker-compose.override.yml after creating local-secrets.
# Files must be readable by web's UID 10001. Never commit real secret files.
services:
  web:
    volumes:
      - type: bind
        source: ./local-secrets
        target: /run/secrets
        read_only: true
        bind:
          create_host_path: false
